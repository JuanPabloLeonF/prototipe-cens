export interface ModelFormularyLogin {
    user: string;
    password: string;
}

export interface ModelFormularyRegister {
    name: string;
    lastName: string;
    contact: string;
    user: string;
    numberMeter: string;
    email: string;
    password: string;
}
